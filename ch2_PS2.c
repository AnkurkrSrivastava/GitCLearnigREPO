#include <stdio.h>

int main(){
    int x=2;
    int y=3,z=3,k=1;
    float i=3*x/y-z+k;
    printf("The value of i is %f and the value of k is %d and the value of z is %d and the value of y is %d and the value of x is %d\n", i, k, z, y, x);
    return 0;
}